# トク退｜退職日計算ナビ — プロジェクトルール

## プロジェクト概要

退職希望日・有休残日数・年収などを入力すると、金銭面で有利な退職日と手取り差額を算出するクライアントサイド完結型シミュレーター。

- 公開URL: https://taishokubi-navi.pages.dev/
- ホスティング: Cloudflare Pages
- 現フェーズ: **β版**（マネタイズ導線は実装しない）

## 仕様書の場所

| ドキュメント | パス |
|---|---|
| システム仕様書（本体） | `docs/tokutai_system_spec_v17_2.md` |
| FAQコンテンツ | `docs/tokutai_faq_content_v1.md` |
| 用語ミニ解説 | `docs/tokutai_glossary_v1.md` |
| フィードバックフォーム設計 | `docs/tokutai_feedback_form_v1.md` |

**実装前に必ず該当章を読むこと。** 記憶や一般論で書かない。

## 技術構成

- Vanilla JS (ES6+) / HTML5 / CSS3。**フレームワーク・ビルドツールは使わない**
- 外部依存ゼロ（CDN・Webフォント・外部API・広告タグをすべて使わない）
  - 例外: ユーザーがボタンを押したときのフィードバック送信のみ
- `localStorage` は `SafeStorage` ラッパー経由でのみ使う
- 計算ロジックは**純粋関数**とし、DOM操作と完全に分離する

```
index.html
css/styles.css
js/
  constants.js        料率・法定金額マスタ
  holidays.js         祝日判定
  calculator.js       試算エンジン（純粋関数のみ）
  faq_master.js       FAQデータ
  glossary.js         用語データ
  schema_generator.js 構造化データ生成
  feedback.js         フィードバック送信
  app.js              UI制御・DOM描画
```

## 絶対に守るルール

### 表現

1. **「免除」という語を社会保険料の文脈で使わない。** 月末退職で保険料は免除されない（発生するが労使折半になる）。必ず両パターンの金額を並べて提示する
2. **「健康保険証」という語を単独で使わない。** 2026年8月1日に完全移行済み。必ず「マイナ保険証・資格確認書」と書く
3. **断定を避ける。** 「〜します」ではなく「〜する場合があります」。認定・判断は必ず公的機関が行う
4. **「永久消滅」「激減」「絶対に」を使わない。** 体調不良のユーザーが読むため
5. **「理想の退職日」と書かない。**「金銭面で有利な退職日」とする
6. **「社労士監修」「専門家が診断」と書かない。** 運営者は現在有資格者ではない

### データ

7. **体調不調フラグ（`inputMentalPhysicalUnfit`）を `localStorage` に保存しない。送信もしない。** 要配慮個人情報にあたる
8. 生年月日・年収の実額・退職金額をフィードバックで送信しない（年代・区分に丸める）
9. Cookie・トラッキング識別子を発行しない

### 実装

10. 料率・法定金額は必ず `constants.js` から参照する。ロジック内に数値を直接書かない
11. 日付処理は `parseSafeDate` / `addMonthsSafe` を必ず経由する（iOS Safari対策・月末加算バグ対策）
12. 法令の条文番号は仕様書 第9章の一覧に従う。記憶で書かない
13. 広告タグ・アフィリエイトリンク・会員登録機能を追加しない（β版では実装しない）

## アクセシビリティ

- 本文16px以上、コントラスト比4.5:1以上、タップターゲット44×44px以上
- ホバーのみで情報を表示しない（スマホにホバーは存在しない）
- インタラクティブ要素は `<button>` を使う。`<span>` + JS は不可
- `outline` を消さない
- 数値入力は `inputmode="numeric"`

## 作業の進め方

- **1度に1ファイルずつ実装する。** 複数ファイルの同時生成はしない
- 計算ロジックを変更したら、仕様書 第12章のテストケースで検証する
- 既存ファイルを書き換える前に、変更内容を要約して確認を取る
- 仕様書に記載のない仕様を推測で追加しない。不明点は質問する

## 料率の更新時期

毎年2〜3月に `constants.js` の値を一次情報（協会けんぽ・日本年金機構・厚生労働省）で確認して更新する。現在の適用年度は**令和8年度（2026年度）**。
