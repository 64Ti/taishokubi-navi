# 『トク退｜退職日計算ナビ』完全製品仕様書 (v5.0)

> 本書は初期版「退職日計算ナビ 完全製品仕様書 (v10.0)」を土台に、その後の全面刷新（トク退 Ver.4.0.0 への切替）と5回の機能改修を反映した、現時点の完全な製品仕様書である。

---

## 1. システム基本情報 & SEO・AIO仕様

- **システム名称**: トク退｜退職日計算ナビ
- **システム目的**: ユーザーが「今考えている退職日」「次の入社予定日」「次回ボーナス支給日」を入力するだけで、法規（健康保険法・厚生年金保険法等）と支給日在籍要件に基づいた「理想の退職日」を算出し、指定日のままだといくら損をするか／理想の日にするといくら得をするかを、有休消化・引き継ぎ・社会保険の空白期間まで含めて一気通貫で可視化する無料シミュレーター。
- **ターゲット層**: 転職・退職を検討中または決定した会社員・公務員（スマホからの利用が9割以上を想定）

### SEO / AIO（AI検索・AI Overviews）対策設定

| 項目 | 内容 |
|---|---|
| ページタイトル | `トク退｜退職日計算ナビ` |
| メタディスクリプション | 退職希望日・有休残日数・給与を入力するだけで、最終出社日・資格喪失日・手取り最大化効果を自動算定。登録不要、完全無料の退職日シミュレーター。 |
| 構造化データ | `WebApplication`（常時）／`HowTo`（診断後、動的生成）／`FAQPage`（診断後、動的生成・状況に応じて設問が増減） |
| クローラー許可 | `robots.txt` で GPTBot / OAI-SearchBot / ChatGPT-User / PerplexityBot / ClaudeBot / Google-Extended を明示的に許可 |
| OGP | `og:image`（1200×630）、`twitter:card=summary_large_image` を設定 |

---

## 2. システム構成・動作要件

- **システム形態**: クライアントサイド完結型 SPA（HTML5 / CSS3 / ES6+ JavaScript）
- **モジュール構成**: `index.html` + `js/holidays.js` + `js/calculator.js` + `js/schema_generator.js` + `js/app.js` + `css/styles.css`
- **外部依存**: **ゼロ**。CDN・Webフォント・外部APIへの通信は一切行わない（Tailwind CDN・Google Fontsは廃止済み。使用クラスをすべて自前CSSに移植）
- **動作要件**:
  - スマホファースト設計（`viewport-fit=cover` 対応）
  - サーバー通信・データベースなし。入力値は `localStorage` にのみ保存（端末外に送信されない）
  - 日付計算は文字列の直接分解パース（`new Date("YYYY-MM-DD")` のタイムゾーン依存を回避）
- **祝日データ**: ハードコード配列ではなく、**アルゴリズムによる動的生成**（西暦2000〜2035年対応）。ハッピーマンデー・春分秋分の近似式・振替休日・国民の休日をすべて計算で導出するため、更新作業が不要。
- **キャッシュ制御**: `_headers` で `Cache-Control: public, max-age=0, must-revalidate` を設定し、更新が即座に反映されるようにしている。

---

## 3. 入力データ仕様（Input）

### STEP 1／日付

| 項目 | ID | UI要素 | 制約 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 退職を希望する日 | `inputResignDate` | `date` | 過去日不可、未入力時は当月末日 | 必須 | 全計算の起点 |
| 次の会社への入社予定日 | `inputNextJoinDate` | `date` | 制約なし | 任意 | 社会保険の空白期間判定・理想の退職日の上限決定 |
| 次回ボーナス支給予定日 | `inputBonusDate` | `date` | 制約なし | 任意 | 支給日在籍要件の判定 |

### STEP 2／休日・有休

| 項目 | ID | UI要素 | 制約 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 毎週の定休日 | `weekdayGrid`（7ボタン） | 曜日フリー選択グリッド | 最低1曜日は選択必須。デフォルト＝土日 | 必須 | 営業日判定（シフト制対応） |
| 祝日の扱い | `holidayOnBtn` / `holidayOffBtn` | 明示的な2択ボタン | ― | 必須 | 国民の祝日を休みに含めるか |
| 現在の有給残日数 | `inputPaidLeave` | `number` | 0〜60 | 必須 | 有休消化逆算 |
| 引き継ぎ必要日数 | `inputHandoverDays` | `number` | 0〜60、デフォルト0 | 任意 | 最終出社日の算定に加算 |
| 会社独自の休み | `extraDateInput` + リスト | `date` 追加式 | 件数無制限 | 任意 | 平日でも休みにする個別日 |
| 会社独自の出勤日 | `extraWorkDateInput` + リスト | `date` 追加式 | 件数無制限 | 任意 | 土日祝でも出勤日にする個別日 |

営業日判定の優先順位：**① 独自の出勤日 > ② 独自の休み > ③ 毎週の定休日・国民の祝日**。同一日を両方に登録した場合は①側へ自動的に移動する。

期間内の祝日は、STEP2の折りたたみ式「期間内の祝日カレンダーを見る」で、今日〜退職希望日の範囲かつ「その人の定休日に当たらない祝日」だけを抽出して一覧表示する。

### STEP 3／給与・進路

| 項目 | ID | UI要素 | 制約 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 去年1年間の年収 | `inputAnnualIncome` | `select`（14区分、250万円未満〜1000万円以上） | 必須 | 必須 | 標準報酬月額の推定（年収÷12で月給概算） |
| ボーナス見込み額 | `inputBonusAmount` | `select`（8区分、5万円未満〜100万円以上） | ボーナス支給日を入力した場合のみ表示・必須 | 条件付き必須 | 損得計算 |
| 健康保険組合の加入有無 | `inputUnionKenpo` | `checkbox` | 任意・結果への影響は補足情報のみ | 任意 | 付加給付の案内 |
| 退職後の進路 | `branchSelector`（3択） | ボタン | ①転職／②療養・無職／③独立 | 必須 | 進路別の文脈型ノート出し分け |

---

## 4. 計算アルゴリズム仕様

### ① 社会保険資格喪失日（健康保険法第36条）

資格喪失日 ＝ 退職日の翌日。月末退職なら当月分の保険料は労使折半、月末より前なら国保・国民年金へ自己全額負担で加入。

### ② 標準報酬月額等級の推定

年収を12等分（賞与込み概算）→ 32等級の簡易テーブル（協会けんぽ令和6年3月分〜を参考）に照合 → 健康保険料率10%・厚生年金料率18.3%（いずれも労使折半後の本人負担概算）で月額負担を算出。

### ③ 有休消化逆算

退職日を「有休消化1日目」として数え、必要営業日数（＝有休日数＋引き継ぎ日数）を退職日から過去へ遡ってカウント。カウントし終えた直前の営業日を「最終出社日」とする。「有休のみ」の消化開始日は別途、退職日翌日を起点とした逆算で算出する。

### ④ 社会保険料の月末最適化

`calcInsuranceMonthlyCost(date, grade)`：月末なら労使折半額（低）、月末以外なら自己全額負担相当（高＝2倍）を返す汎用関数。任意の2つの退職日候補の保険料差を機械的に比較できる。

### ⑤ ボーナス（賞与）の支給日在籍判定

`calcBonusImpact(date, bonusDate, bonusAmount)`：判定対象の退職日が支給日以降であれば受給、それより前なら対象外と判定する（支給日在籍要件）。

### ⑥ 社会保険の空白期間判定

`calcInsuranceGap(resignDateForGap, nextJoinDate)`：資格喪失日（退職日翌日）と次の入社日を比較し、`none`（即時移行）／`gap`（空白日数を算出）／`overlap`（重複、要見直し）の3状態を返す。

### ⑦ 理想の退職日の統合判定（本アプリの中核ロジック）

`calcRecommendedResignDate(resignDate, nextJoinDate, insuranceOptimization, bonusDate)` が以下の優先順位で単一の結論を出す。

1. **絶対条件**：次の入社日が確定していれば、その前日を超える退職日は選べない（`limitedByNextJob` で明示）
2. **ボーナス優先**：ボーナス支給日が①の範囲内に収まるなら、支給日をまたぐ直後の月末まで繰り下げる（ボーナス額は通常、社会保険料の差額より大きいため優先）。①の範囲外なら `bonusConflict: 'unreachable'` として明示的に「間に合わない」ことを伝える
3. **保険料最適化**：上記いずれの制約もなければ、社会保険料が最適な月末退職日を推奨する

### ⑧ 退職日を変えることによる損得（得/損の明確化）

`calcResignDateGainLoss(userResignDate, recommendedDate, grade, bonusDate, bonusAmount)` が、指定日のまま退職した場合と理想の日にした場合の金銭差を機械的に算出する。

- `insuranceDelta`：社会保険料の差（正＝理想日の方が安い）
- `bonusDelta`：ボーナス受給有無の差（正＝理想日の方が受け取れる）
- `totalDelta`：上記合計。結果画面では「理想の日にすると約◯円お得／損」として提示

有給休暇の金銭的価値（日給概算×消化日数）は、退職日をどちらにしても消化日数が同じであれば変わらないため、この損得計算には含めず、参考情報として分離して表示する。

---

## 5. 出力・画面表示仕様（Output）

### 画面フロー

```
[STEP 1: 退職日・入社日・ボーナス日]
        ↓
[STEP 2: 定休日・祝日・有休・独自休日]  ← ライブプレビュー（指定日→理想の日の速報）
        ↓
[STEP 3: 年収・ボーナス額・組合健保・進路]
        ↓
[診断結果画面]
```

進捗バーとステップラベル（STEP n/3）を画面上部に常時表示。「戻る」で前ステップへ、各ステップの必須項目が揃うまで「次へ」は無効化される。

### STEP 2 ライブプレビュー（金額確定前の速報）

有休日数が入力された時点で、以下を即時表示：

- 最終出社日
- 指定した退職日 → 理想の退職日（給与未入力のため金額は出さず日付のみ）
- 理想の日にする理由（既に最適／月末推奨／入社日制約／ボーナスまたぎ、の4パターン）

### 診断結果画面

1. **見出しカード（ゴールドグラデーション）**
   - 「あなたの理想の退職日」＋大きく日付を表示
   - 一言の結論文（4パターン：現状維持でOK／ボーナス確保のため繰り下げ推奨／入社日制約で調整／保険料最適化推奨）
   - 指定した退職日 → 理想の退職日 の比較行（同一日なら非表示）
   - 空白期間の注記（3状態）
   - ボーナス受給可否の注記（3状態：確保済み／今のままだと逃す／制約で不可）
   - **退職日を変えた場合の損得**：総額（お得／損／差なし）＋内訳（社会保険料の差・ボーナスの有無の差）
   - 有休消化価値（参考情報として分離、損得には非算入）
2. **ToDoタイムライン**（進路によって内容が変化）
   - 離職票の到着待ち（常設・注意）
   - 最終出社日
   - 有給休暇の消化開始
   - 社会保険 資格喪失日
   - 空白期間の手続き（該当時のみ）
   - 進路別の Critical Alert × 解決アクション（療養・独立）
   - 進路別の文脈型PR導線（転職・療養・独立）
   - 進路別の一般的な注意（転職：住民税一括徴収・空白期間）
3. **Xシェアボタン**：理想の退職日・最終出社日・資格喪失日・損得サマリーを含む投稿文を生成
4. **やり直しボタン**：全入力・localStorageをリセット

---

## 6. ファイル構成

```
taishokubi_navi/
├── index.html              メインHTML（3ステップUI + 結果画面）
├── css/
│   └── styles.css          自前CSS（旧Tailwind CDN代替、全171クラス相当を手書き実装）
├── js/
│   ├── holidays.js         祝日判定・営業日チェッカー・期間内祝日抽出
│   ├── calculator.js       試算エンジン本体（日付計算・保険料・損得計算）
│   ├── schema_generator.js JSON-LD動的生成（WebApplication/HowTo/FAQPage）
│   └── app.js               UI制御・状態管理・localStorage永続化
├── _headers                 Cloudflare Pages のキャッシュ制御
├── robots.txt / sitemap.xml SEO
├── ogp.png / apple-touch-icon.png / icon-*.png
├── README.md                 保守手順
├── ROADMAP.md                 公開までの手順（達成済み）
└── SPEC.md                    本書
```

---

## 7. 保守・運用要件

| 項目 | 内容 | 場所 |
|---|---|---|
| 祝日データ | アルゴリズム生成のため更新不要（西暦2000〜2035年対応） | `js/holidays.js` |
| 健康保険料率 | 例年3月分（4月納付分）から改定される可能性 | `js/calculator.js` の料率定数（0.05 / 0.0915） |
| 標準報酬月額等級表 | 協会けんぽの改定時に差し替え | `js/calculator.js` の `STANDARD_REMUNERATION_TABLE` |
| 公開URL | `https://taishokubi-navi.pages.dev/`（独自ドメイン未取得、`.pages.dev` 無料枠で運用中） | `index.html` 冒頭のマーカー内、`js/schema_generator.js` の `SITE_URL` |

---

## 8. 変更履歴

| バージョン | 内容 |
|---|---|
| v10.0 → v10.1 | 単一HTMLで初期実装。祝日ハードコード配列・標準報酬月額50等級・社会保険料実額計算 |
| v10.1 → デザイン刷新 | 国内SaaS水準のUI/UXへブラッシュアップ（ネイビー基調、ガラスモフィズム等） |
| **v4.0.0（トク退へ全面刷新）** | ユーザー作成プログラムのZIPを取り込み、単一HTMLから4モジュール構成へ全面移行。3ステップウィザード＋ToDoタイムライン形式に刷新。祝日データをハードコード配列からアルゴリズム生成へ変更 |
| 改修1 | 有休消化のオフバイワン修正／会社独自の出勤日・引き継ぎ日数を復元／Tailwind CDN・Google Fontsを撤去し外部依存ゼロを回復 |
| 改修2 | 額面月給→年収選択制に変更／死んでいた保険選択UIを削除／結果画面の見出しを金額から日付の結論へ再構成／次の入社日入力を復活・空白期間判定と統合 |
| 改修3 | 定休日を曜日フリー選択に変更（シフト制対応）／期間内祝日カレンダーの可視化／STEP2ライブプレビューへの理想日先出し表示／折りたたみUIの導入 |
| **v5.0（本書時点）** | 祝日トグルを明示的な2択ボタンへ／指定日と理想日の比較を全画面で統一表示／「手取り最大化インパクト」を廃止し損得（お得／損）を明確化／ボーナス支給日在籍要件を統合ロジックに組み込み |

---

## 9. 免責事項

本シミュレーションは一般的な法令に基づく概算であり、個別の最終判定は管轄のハローワーク・年金事務所・健康保険組合が行う。標準報酬月額等級・保険料率は簡易版であり、都道府県・加入する健康保険組合により実際の金額は異なる。年収から月給への換算（12等分）は賞与比率が高いほど実額より高めに出る。本ページはプロモーション（PR）を含み、提携リンク経由の申し込みで紹介料が発生する場合がある。
