# 『トク退｜退職日計算ナビ』完全製品仕様書 (v7.0)

> 本書は「完全製品仕様書 (v5.0)」を土台に、専門家5視点フィードバックを反映した v6.0、
> およびその後の実運用フィードバック（進路別出口の差別化不足・タイムラインの分かりにくさ・
> 入社日入力の論理不整合・ボーナス入力の曖昧さ）を反映した v7.0 時点の完全な製品仕様書である。

---

## 1. システム基本情報 & SEO・AIO仕様

- **システム名称**: トク退｜退職日計算ナビ
- **システム目的**: ユーザーが「今考えている退職日」「（転職の場合のみ）次の入社予定日」「直近1回分のボーナス支給日」を入力するだけで、法規（健康保険法・厚生年金保険法等）と支給日在籍要件に基づいた「理想の退職日」を算出し、指定日のままだといくら損をするか／理想の日にするといくら得をするかを可視化し、進路（転職／療養・無職／独立）別に異なる実務期限を含む番号付きのアクションタイムラインを提示する無料シミュレーター。
- **ターゲット層**: 転職・退職を検討中または決定した会社員・公務員（スマホからの利用が9割以上を想定）

### SEO / AIO（AI検索・AI Overviews）対策設定

| 項目 | 内容 |
|---|---|
| ページタイトル | `トク退｜退職日計算ナビ` |
| メタディスクリプション | 退職希望日・有休残日数・給与を入力するだけで、最終出社日・資格喪失日・手取り最大化効果を自動算定。登録不要、完全無料の退職日シミュレーター。 |
| 構造化データ（JSON-LD） | `SoftwareApplication`（常時）／ `HowTo`・`FAQPage`（診断結果に応じ `schema_generator.js` で動的DOM挿入。就業規則アドバイス・空白期間手続き・健康保険種別・ボーナス支給日在籍要件のQ&Aを条件付きで追加） |
| クローラー許可 | `robots.txt` で GPTBot / OAI-SearchBot / ChatGPT-User / PerplexityBot / ClaudeBot / Google-Extended を明示的に許可 |
| OGP | `og:image`（1200×630）、`twitter:card=summary_large_image` を設定 |
| AIO引用ブロック | STEP1冒頭に、LLM参照（Retrieval Citation）用の視認可能なアンサーファースト文（`.aio-answer`）をセマンティック配置 |

---

## 2. システム構成・動作要件

- **システム形態**: クライアントサイド完結型 SPA（HTML5 / CSS3 / ES6+ JavaScript）
- **モジュール構成**: `index.html` + `js/holidays.js` + `js/calculator.js` + `js/schema_generator.js` + `js/app.js` + `css/styles.css`
- **外部依存**: **ゼロ**。CDN・Webフォント・外部API通信は完全非依存（Tailwind CDN・Google Fonts撤去済み）
- **動作要件**:
  - スマホファースト設計（`viewport-fit=cover` 対応）
  - サーバー通信・データベースなし。入力値は `localStorage`（キー: `tokutai_form_state`）へ自動保存・復元
  - iOS Safari互換の日付パース処理（`parseSafeDate`）により、ハイフン/スラッシュ区切り文字列の時差・`NaN` バグを防止。旧名 `parseDate` は互換のためのエイリアス
  - 純粋関数（Pure Function）による計算ロジックとDOM描画の分離
- **祝日データ**: ハードコード配列不要のアルゴリズム生成（西暦2000〜2035年対応）。年ごとにMapキャッシュして高速判定。
- **キャッシュ制御**: `_headers` で `Cache-Control: public, max-age=0, must-revalidate` を設定。

---

## 3. 入力データ仕様（Input）

### STEP 1／日付（EFO改善: クイックチップ／AIO引用ブロック）

| 項目 | ID | UI要素 | 制約・補助機能 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 退職を希望する日 | `inputResignDate` | `date` + クイックチップ | 過去日不可。「2週間後」「今月末」「来月末」ボタン | 必須 | 全計算の起点 |
| 直近1回分のボーナス支給予定日 | `inputBonusDate` | `date` | 制約なし。**今後何度も支給される場合は、退職検討時期に一番近い1回分だけを入力する**旨をヘルプ文で明記 | 任意 | 支給日在籍要件の判定 |

> ⚠️ v6.0までは「次の会社への入社予定日」もSTEP1にあったが、v7.0でSTEP3（進路選択後）へ移動した（詳細は §4-⑦ を参照）。

### STEP 2／休日・有休

| 項目 | ID | UI要素 | 制約 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 毎週の定休日 | `weekdayGrid`（7ボタン） | 曜日フリー選択グリッド | 最低1曜日選択。デフォルト＝土日 | 必須 | 営業日判定（シフト制対応） |
| 祝日の扱い | `holidayOnBtn` / `holidayOffBtn` | 明示的な2択ボタン | ― | 必須 | 国民の祝日を休みに含めるか |
| 現在の有給残日数 | `inputPaidLeave` | `number` | 0〜60 | 必須 | 有休消化逆算 |
| 引き継ぎ必要日数 | `inputHandoverDays` | `number` | 0〜60、デフォルト0 | 任意 | 最終出社日の算定に加算 |
| 会社独自の休み | `extraDateInput` + リスト | `date` 追加式 | 件数無制限 | 任意 | 平日でも休みにする個別日 |
| 会社独自の出勤日 | `extraWorkDateInput` + リスト | `date` 追加式 | 件数無制限 | 任意 | 土日祝でも出勤日にする個別日 |

営業日判定の優先順位：**① 独自の出勤日 > ② 独自の休み > ③ 毎週の定休日・国民の祝日**。

STEP2のライブプレビューでは、指定した退職日と理想の退職日（保険料・ボーナスの日付情報のみで暫定計算、金額はSTEP3後に確定）を比較表示する。

### STEP 3／給与・保険・進路

| 項目 | ID | UI要素 | 制約 | 必須/任意 | 用途 |
|---|---|---|---|---|---|
| 去年1年間の年収 | `inputAnnualIncome` | `select`（14区分、250万円未満〜1000万円以上） | 必須 | 必須 | 標準報酬月額の推定 |
| その直近1回分のボーナス見込み額 | `inputBonusAmount` | `select`（8区分、5万円未満〜100万円以上） | ボーナス支給日入力時のみ表示 | 条件付き必須 | 損得計算 |
| 加入中の健康保険 | `inputInsuranceType` | `select` | 協会けんぽ／組合健保／共済組合（公務員等） | 必須 | 組合健保の付加給付・共済組合の独自規定注記の分岐 |
| 退職後の進路 | `branchSelector`（3択） | ボタン | ①転職／②療養・無職／③独立 | 必須 | 進路別の実務ステップ・動的オファーマトリクス切替 |
| 次の会社への入社予定日 | `inputNextJoinDate` | `date`（`nextJoinDateField`、進路選択の直後に配置） | **「①転職」を選んだときだけ表示**。他の進路に切り替えると入力欄ごと隠れ、値もクリアされる | 転職選択時のみ・任意 | 空白期間判定・理想の退職日の上限決定 |

---

## 4. 計算アルゴリズム & エンジン仕様

### ① iOS Safari完全互換日付処理 (`parseSafeDate`)

ハイフン/スラッシュ区切り文字列を数値分解してローカルタイムゾーンの `Date` を安全生成。空文字・不正な区切りの場合は現在時刻の `Date` を返し、例外を投げない。

### ② 社会保険資格喪失日（健康保険法第36条）

資格喪失日 ＝ 退職日の翌日。月末退職なら当月分保険料は労使折半、月末前日以前なら国保・国民年金へ自己全額負担で加入。健康保険の種別（協会けんぽ／組合健保／共済組合）ごとに異なる補足注記を返す。

### ③ 有休消化・営業日逆算

退職日を「有休消化1日目」として必要営業日数（有休＋引き継ぎ）を過去へ遡り、最終出社日および有休開始日を特定。

> ⚠️ **v7.0での重要な修正**：この逆算の起点は「ユーザーが指定した退職日」ではなく **「理想の退職日」** に統一した。v6.0までは指定日を起点にしていたため、理想の退職日が12/31でも資格喪失日が指定日（例：9/15）の翌日と表示される不整合があった。

### ④ 上司へ切り出す目安日（`calcNoticeDate`、v7.0新設）

理想の退職日の1か月前（月末クランプ済み）を基本とするが、有休消化日数が多く最終出社日の方が早く来る場合は、在籍しているうちに伝える必要があるため**最終出社日を目安日として採用**する（`clampedByLastWorkDay` フラグで判定を明示）。

### ⑤ 理想の退職日統合判定 & 損失回避計算

1. **制約**: 次の入社日（転職を選んだ場合のみ）の前日を超えない（`limitedByNextJob`）
2. **ボーナス優先**: 制約の範囲内ならボーナス支給日後の月末へ繰り下げ。範囲外なら `bonusConflict: 'unreachable'`
3. **保険料最適化**: 上記制約がなければ最寄りの月末を推奨
4. **損得算出**: 社会保険料差＋ボーナス受給差＝「放置すると損する金額（損失回避額）」を算出

### ⑥ 進路別の実務期限（v7.0新設。§5-③の「出口の差別化」の中核）

| 進路 | 関数 | 内容 |
|---|---|---|
| 療養・無職 | `calcInsuranceSwitchDeadline(lossDate)` | 国民健康保険・国民年金の切替期限＝資格喪失日から原則14日以内 |
| 独立 | `calcBlueFormDeadline(resignDate)` | 青色申告承認申請書の提出期限＝開業日（退職日翌日を仮定）から2か月以内。ただし1/1〜1/15開業の場合のみその年の3/15までの特例を適用（実装時に条件式の逆転バグを発見・修正済み） |
| 転職 | ―（`nextJoinDate` をそのままタイムラインに反映） | 次の会社で働き始める日を、空白期間の有無とセットでタイムラインに明示 |

---

## 5. 出力・画面表示 & パーソナライズマーケティング仕様

### 画面フロー

```
[STEP 1: 退職日 + ボーナス日]
        ↓
[STEP 2: 定休日・祝日・有休・独自休日]  ← ライブプレビュー
        ↓
[STEP 3: 年収・ボーナス額・健康保険・進路 →（転職のみ）入社日]
        ↓
[診断結果画面]
```

### ① 診断結果：見出しカード（ゴールドグラデーション）

- **損失回避コピー**（プロスペクト理論）：差額があるときのみ「【注意】退職日を1日間違えるだけで、最大〇〇円をドブに捨てることになります」を表示。差がゼロ（既に最適）のときは安心コピーに自動切替
- **進路ラベル**：「💼 転職される方向けの診断結果です」等、診断が誰向けかを冒頭で明示
- 「あなたの理想の退職日」＋大きく日付
- 指定した退職日 → 理想の退職日 の比較行（同一日なら非表示）
- 空白期間の注記（3状態）／ボーナス受給可否の注記（3状態）
- **退職日を変えた場合の損得**：総額＋内訳（社会保険料の差・ボーナスの有無の差）
- 有休消化価値（参考情報として分離。日付を変えても金額が変わらないため損得計算には非算入）

### ② ToDoタイムライン（v7.0で全面再設計）

**旧デザインの課題**：カードが単純に縦積みされ、番号も接続線もなく「見にくい」というフィードバックがあった。また進路（転職／療養／独立）による差分がタイムライン全体の1〜2割程度しかなく「出口が変わらない」という指摘があった。

**新デザイン**：`.tl-item`（番号ノード＋接続線＋日付付きカード）を縦に連結した、実際のチェーン状タイムライン。各アクションは「具体的な日付 + 見出し + 説明」の1項目1アクションで構成する。

| # | アクション | 日付根拠 |
|---|---|---|
| 1 | 上司に相談する | `calcNoticeDate`（理想の退職日の1か月前、最終出社日でクランプ） |
| 2 | 最終出社日を迎える | 理想の退職日から有休＋引継ぎ日数を逆算 |
| 3 | 有給休暇の消化を開始 | 最終出社日の翌営業日〜 |
| 4 | 理想の退職日を迎える | `calcRecommendedResignDate` の結果（ゴールド強調） |
| 5 | 社会保険 資格喪失日 | 理想の退職日の翌日（クリムゾン強調） |
| 6 | **進路別ステップ**（転職＝次の入社日／療養＝国保切替期限／独立＝青色申告期限） | §4-⑥ |
| 7 | 離職票の到着を待つ | 資格喪失日から10日後〜14日後の**日付レンジ**で提示 |

番号を持たない補足カード（Critical Alert × Solution ペア、進路別PR導線、一般Caution）は `.tl-aside`（字下げ）で直前の関連アクションに従属させ、視覚的にメインの時系列と区別する。

進路別動的パーソナライズ・オファーマトリクス（PR表記つき）：

| 進路選択 | 訴求キャッチコピー | 提携案件カテゴリ |
| :--- | :--- | :--- |
| **① 転職** | 「有休消化中の2週間で次のキャリアのスカウトを受け取る」 | 転職スカウト・退職代行 |
| **② 療養・無職** | 「自己都合退職でも、条件を満たせば傷病手当金や失業給付などの公的な給付金を受け取れる場合があります」 | 傷病手当金・給付金サポート |
| **③ 独立** | 「国保より安くなる？個人事業主の社会保険料を見直しつつ、無料で開業届を作成する」 | freee開業 / マネーフォワード開業 |

> 📝 v6.0仕様書原案の療養・無職向けコピーには「国から最大28ヶ月の給付金」という記載があったが、傷病手当金の上限は最長1年6ヶ月（18ヶ月）、雇用保険の基本手当も所定給付日数は数百日単位であり、単純合算しても28ヶ月には届かない根拠不明な数値だったため、誇張のない表現に修正して実装している。

### ③ その他のCTA

- **LINE公式アカウント案内**：「診断結果の保存・個別カレンダー作成はLINEで受け取る」。実際のLINE公式アカウントは未接続のため、外部リンクは開かず「準備中」の案内シートを表示するのみ（実在しないリンクは作らない）
- **Xシェアボタン**：理想の退職日・最終出社日・資格喪失日・損得サマリーを含む投稿文を生成
- **やり直しボタン**：全入力・localStorageをリセット

---

## 6. 免責事項（Legal Disclaimer）

> **【免責事項・注意事項】**
> 本シミュレーションによる試算結果は、労働基準法、民法、健康保険法および厚生年金保険法等の関係法令に基づき算出された**概算値**であり、個別の雇用契約内容、就業規則、労使協定、または所轄労働基準監督署・年金事務所の判断により異なる場合があります。本ツールの利用により生じた直接的・間接的トラブルや損害について、当サービス運営者は一切の法的責任を負いません。最終的な退職日および社会保険の引き継ぎ手続きについては、必ず勤務先の労務担当者または管轄の年金事務所・ハローワークへご確認ください。
>
> ※本ページはプロモーション（PR）を含みます。提携リンク経由のお申し込みにより、当サイトに紹介料が発生する場合があります。

---

## 7. ファイル構成

```
taishokubi_navi/
├── index.html              メインHTML（3ステップUI + 結果画面）
├── css/
│   └── styles.css          自前CSS（Tailwind CDN代替。タイムライン用 .tl-* クラス等を含む）
├── js/
│   ├── holidays.js         祝日判定・営業日チェッカー・期間内祝日抽出
│   ├── calculator.js       試算エンジン本体（日付計算・保険料・損得・進路別実務期限）
│   ├── schema_generator.js JSON-LD動的生成（SoftwareApplication/HowTo/FAQPage）
│   └── app.js               UI制御・状態管理・localStorage永続化
├── _headers                 Cloudflare Pages のキャッシュ制御
├── robots.txt / sitemap.xml SEO
├── ogp.png / apple-touch-icon.png / icon-*.png
├── README.md                 保守手順
├── ROADMAP.md                 公開までの手順（達成済み）
└── SPEC.md                    本書
```

---

## 8. 保守・運用要件

| 項目 | 内容 | 場所 |
|---|---|---|
| 祝日データ | アルゴリズム生成のため更新不要（西暦2000〜2035年対応） | `js/holidays.js` |
| 健康保険料率 | 例年3月分（4月納付分）から改定される可能性 | `js/calculator.js` の料率定数（0.05 / 0.0915） |
| 標準報酬月額等級表 | 協会けんぽの改定時に差し替え | `js/calculator.js` の `STANDARD_REMUNERATION_TABLE` |
| localStorageキー | `tokutai_form_state`（v6.0で `tokutai_navi_state_v4` から変更） | `js/app.js` の `STORAGE_KEY` |
| 公開URL | `https://taishokubi-navi.pages.dev/`（独自ドメイン未取得、`.pages.dev` 無料枠で運用中） | `index.html` 冒頭のマーカー内、`js/schema_generator.js` の `SITE_URL` |
| LINE公式アカウント | 未接続。開設後は `js/app.js` の `onLineCV` 関数に実URLを追加する必要あり | `js/app.js` |

---

## 9. 変更履歴

| バージョン | 内容 |
|---|---|
| v10.0 → v10.1 | 単一HTMLで初期実装。祝日ハードコード配列・標準報酬月額50等級・社会保険料実額計算 |
| v10.1 → デザイン刷新 | 国内SaaS水準のUI/UXへブラッシュアップ |
| v4.0.0（トク退へ全面刷新） | 単一HTMLから4モジュール構成へ移行。3ステップ形式＋ToDoタイムライン実装 |
| v5.0 | 祝日2択ボタン化、損得（お得/損）明確化、ボーナス支給日在籍要件統合 |
| v6.0 | `parseSafeDate`、クイック日付チップ、入力自動保存/復元、動的JSON-LD、プロスペクト理論損失コピー、進路別オファーマトリクス、健康保険3択、法的免責事項の厳密化 |
| **v7.0（本書）** | 次の入社日入力を進路選択後（転職選択時のみ）に移動し論理的整合性を確保／ToDoタイムラインを番号付き・日付付きの接続デザインへ全面刷新／進路別の実務期限（療養＝国保切替期限、独立＝青色申告期限、転職＝入社日反映）を新設し進路ごとの出口を実質的に差別化／ボーナス入力を「直近1回分」であることが明確な文言に修正／**最終出社日・資格喪失日の算定基準を「指定日」から「理想の退職日」に統一する整合性バグを修正**（従来は理想の退職日と資格喪失日の表示が矛盾していた） |

